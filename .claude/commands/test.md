# Run Tests

Run all tests for the embedmit package using devtools::test().

```bash
cd /Users/rmsharp/Documents/R_packages/embedmit && Rscript -e "devtools::test()"
```

After running, report the test results summary including number of passed, failed, skipped, and any warnings.
